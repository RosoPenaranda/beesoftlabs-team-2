export enum UserRole {
  CLIENT = 'client',
  EMPLOYEE = 'employee',
  ADMIN = 'admin',
}

export enum PetSize {
  TINY = 'tiny',
  NORMAL = 'medium',
  BIG = 'big',
}

export enum TypeOfService {
  PASEO = 'paseo',
  PELUQUERIA = 'peluqueria',
  GUARDERIA = 'guarderia',
}
